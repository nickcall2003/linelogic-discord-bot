# Discord Developer Portal -> your app -> Bot -> Reset Token
DISCORD_BOT_TOKEN=

# Enable Developer Mode (User Settings -> Advanced), then right-click your
# server icon -> Copy Server ID
DISCORD_GUILD_ID=

# Your existing FastAPI backend. The bot calls these real routes:
#   /api/model                    (/model — after you paste in backend_model_endpoint.py)
#   /api/picks/best               (/today)
#   /api/parlays/record + /api/clv (/record)
#   /api/stocks/quote + /api/stocks/hotpick (/stock)
# Use whatever base serves those — likely https://thelinelogic.com (no /api suffix;
# the paths above already include /api). Adjust if your API is on a subdomain.
LINE_LOGIC_API_BASE=https://thelinelogic.com
LINE_LOGIC_API_KEY=

# Any random long string — must match what your backend sends in the
# X-Webhook-Secret header
WEBHOOK_SHARED_SECRET=

# Right-click each channel -> Copy Channel ID
CHANNEL_DAILY_MODEL=
CHANNEL_OFFICIAL_RESULTS=
CHANNEL_WELCOME=
CHANNEL_BOT_LOGS=

# Right-click the Verified Member role -> Copy Role ID
VERIFIED_ROLE_ID=

# Right-click each notify role in Server Settings -> Roles -> Copy Role ID
# Names in your server: "Baseball Alerts", "Basketball Alerts", etc. The env var
# name is what matters to the bot, not the role's display name.
ROLE_MLB_ALERTS=
ROLE_NBA_ALERTS=
ROLE_NFL_ALERTS=
ROLE_NHL_ALERTS=
ROLE_NCAAF_ALERTS=
ROLE_NCAAB_ALERTS=
ROLE_WNBA_ALERTS=
ROLE_TENNIS_ALERTS=
ROLE_SOCCER_ALERTS=
ROLE_UFC_ALERTS=
ROLE_STOCK_ALERTS=
ROLE_LINE_MOVEMENT_ALERTS=

# Hex color to match thelinelogic.com brand palette, no leading #
BRAND_COLOR_HEX=2B6CB0

# Railway sets this automatically
PORT=8080

# --- AI explain layer — leave off until the backend /api/explain route is live ---
# The LLM key lives on the BACKEND, never here. On the backend set ANTHROPIC_API_KEY
# (Claude Haiku is the default and cheapest for write-ups). Set this bot flag to 1
# only after that route is deployed.
AI_EXPLAIN_ENABLED=0

# --- Not needed yet — leave blank until you activate Whop/premium ---
PREMIUM_ROLE_ID=
